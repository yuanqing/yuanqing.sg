{
  "private": true,
  "type": "module",
  "scripts": {
    "build": "yarn run clean-build && yarn run build-html && yarn run build-css && yarn run build-media && cpy CNAME build",
    "build-css": "postcss src/css/style.css --dir build --no-map --use cssnano",
    "build-html": "node --loader tsm scripts/build-html.ts",
    "build-media": "imagemin 'media/**/*' --out-dir build/media",
    "clean": "rm -rf '*.log' && yarn run clean-build && yarn run clean-stats",
    "clean-build": "rm -rf build",
    "clean-stats": "rm -rf data.json",
    "fix": "concurrently --raw 'yarn run fix-css' 'yarn run fix-html' 'yarn run fix-ts'",
    "fix-css": "stylelint --fix 'src/**/*.css'",
    "fix-html": "node --loader tsm scripts/sort-html-classes.ts && prettier 'src/**/*.html' --loglevel error --print-width 9999 --write",
    "fix-ts": "eslint --fix 'scripts/**/*.ts'",
    "lint": "concurrently --raw 'yarn run lint-css' 'yarn run lint-ts'",
    "lint-css": "node --loader tsm scripts/check-unused-css.ts 'src/**/*.html' 'src/**/*.css' && stylelint 'src/**/*.css'",
    "lint-ts": "eslint 'scripts/**/*.ts'",
    "open": "open-cli http://0.0.0.0:4242",
    "postinstall": "simple-git-hooks",
    "reset": "yarn run clean && rm -rf node_modules yarn.lock && yarn install",
    "serve": "mkdirp build && sirv build --dev --host --port 4242 --quiet",
    "stats": "yarn run clean-stats && node --loader tsm --require dotenv/config scripts/fetch-stats/fetch-stats.ts",
    "watch": "yarn run clean-build && concurrently --raw 'yarn run serve' 'yarn run watch-css' 'yarn run watch-html' 'yarn run watch-media'",
    "watch-css": "chokidar 'src/css/**/*.css' --command 'yarn run build-css' --initial --silent",
    "watch-html": "chokidar 'src/html/**/*.html' --command 'yarn run build-html' --initial --silent",
    "watch-media": "chokidar 'media/**/*' --command 'yarn run build-media' --initial --silent"
  },
  "devDependencies": {
    "@octokit/rest": "^18.12.0",
    "@types/fs-extra": "^9.0.13",
    "@types/jsdom": "^16.2.13",
    "@types/node": "^16.11.7",
    "chokidar-cli": "^3.0.0",
    "concurrently": "^6.4.0",
    "cpy-cli": "^3.1.1",
    "cssnano": "^5.0.10",
    "dotenv": "^10.0.0",
    "eslint": "^8.2.0",
    "eslint-config-yuanqing": "^0.0.6",
    "fs-extra": "^10.0.0",
    "globby": "^12.0.2",
    "html-minifier": "^4.0.0",
    "imagemin-cli": "^7.0.0",
    "isomorphic-unfetch": "^3.1.0",
    "lint-staged": "^12.0.2",
    "lodash.template": "^4.5.0",
    "mkdirp": "^1.0.4",
    "open-cli": "^7.0.1",
    "p-props": "^5.0.0",
    "postcss": "^8.3.11",
    "postcss-cli": "^9.0.2",
    "prettier": "^2.4.1",
    "puppeteer": "^11.0.0",
    "purify-css": "^1.2.5",
    "simple-git-hooks": "^2.7.0",
    "sirv-cli": "^1.0.14",
    "standard": "^16.0.4",
    "stylelint": "^14.1.0",
    "stylelint-config-yuanqing": "^0.0.3",
    "tsm": "^2.1.4",
    "typescript": "^4.4.4"
  },
  "eslintConfig": {
    "extends": "eslint-config-yuanqing"
  },
  "lint-staged": {
    "*.css": [
      "stylelint"
    ],
    "*.ts": [
      "eslint"
    ]
  },
  "prettier": "eslint-config-yuanqing/prettier",
  "simple-git-hooks": {
    "pre-commit": "yarn run lint-staged",
    "pre-push": "yarn run lint && yarn run fix && yarn run build"
  },
  "stylelint": {
    "extends": "stylelint-config-yuanqing"
  }
}
